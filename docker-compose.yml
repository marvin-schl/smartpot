version: "3.3" 
services:
  smartpot:
    user: "0:1000"
    build:
      context: .
      dockerfile: dockerfiles/dockerfile_smartpot
    privileged: true
    volumes:
    - type: bind
      source: ./smartpot.ini
      target: /home/smartpot/smartpot.ini
    - type: bind
      source: ./smartpot.log
      target: /home/smartpot/smartpot.log
    - type: bind
      source: ./Logging.csv
      target: /home/smartpot/Logging.csv
  nodered:
    user: "0:1000"
    build:
      context: .
      dockerfile: dockerfiles/dockerfile_nodered
    ports:
      - "1880:1880"
    volumes:
      - ./Logging.csv:/home/pi/Logging.csv
      - nodered_data:/data
volumes:
  nodered_data:
